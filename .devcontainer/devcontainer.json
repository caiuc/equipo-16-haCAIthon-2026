{
  "name": "OnlyUs · HaCAithon 2026",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "forwardPorts": [
    4000,
    5173
  ],
  "portsAttributes": {
    "4000": {
      "label": "Servicio 4000",
      "onAutoForward": "silent"
    },
    "5173": {
      "label": "Aplicación",
      "onAutoForward": "openPreview"
    }
  },
  "postAttachCommand": {
    "app": "bash -lc 'cd backend && npm run dev & cd frontend && npm run dev -- --host 0.0.0.0; wait'"
  },
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'sudo service postgresql start; sudo -u postgres psql -c \"ALTER USER postgres PASSWORD '\\''postgres'\\'';\"; sudo -u postgres createdb hacaithon_db || true; cd backend && npm install && npm run seed || true; cd ../frontend && npm install'",
  "features": {
    "ghcr.io/itsmechlark/features/postgresql:1": {
      "version": "16"
    }
  }
}
